/**
 * MCP Server 模式（Phase 3 引入）。
 *
 * Phase 0 仅占位 barrel。届时实现：
 *  - `pingcode mcp serve`：启动 stdio MCP Server
 *  - 三层 tool 注册策略（核心 30 始终注册 / 按需 --include / 长尾仅 CLI）
 */

/** MCP 模块占位版本 */
export const MCP_VERSION = '0.0.0-placeholder';
